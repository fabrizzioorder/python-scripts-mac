'''
CircularArray**

class supports an array-like data structures which can be efficiently rotated.
If possible, the class should use a generic type (aka template) and should
    support iteration via the standard for (Obj o : circularArray) notation
'''